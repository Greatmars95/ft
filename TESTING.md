# 🧪 Тестирование проекта FT

## 📋 Что у нас есть

### Unit тесты
- **FT сервис** (`main_test.go`) - 5 тестов для gRPC сервера
- **HT сервис** (`ht/main_test.go`) - 5 тестов для HTTP gateway

### GitHub Actions CI/CD
- Автоматическое тестирование при каждом push
- Проверка форматирования кода
- Интеграционные тесты
- Сборка Docker образов

---

## 🚀 Как запустить тесты локально

### 1. Запустить все тесты

```bash
# Тесты FT сервиса
go test -v ./...

# Тесты HT сервиса
cd ht && go test -v ./...
```

### 2. Тесты с покрытием кода

```bash
# FT сервис
go test -cover ./...

# HT сервис
cd ht && go test -cover ./...
```

### 3. Детальный отчёт покрытия

```bash
# Генерируем отчёт
go test -coverprofile=coverage.out ./...

# Смотрим в консоли
go tool cover -func=coverage.out

# Открываем в браузере
go tool cover -html=coverage.out
```

### 4. Проверка форматирования кода

```bash
# Проверить форматирование
gofmt -l .

# Автоматически отформатировать
gofmt -w .
```

---

## 🔄 GitHub Actions

### Что происходит при каждом push:

1. ✅ Установка Go и зависимостей
2. ✅ Генерация proto файлов
3. ✅ Запуск unit тестов (FT + HT)
4. ✅ Проверка форматирования кода
5. ✅ Сборка Docker образов
6. ✅ Интеграционный тест (curl к /quotes)

### Где смотреть результаты:

1. Зайти на GitHub
2. Перейти в репозиторий
3. Вкладка **Actions**
4. Выбрать нужный workflow

---

## 📊 Что тестируют наши тесты

### FT сервис (main_test.go):

| Тест | Что проверяет |
|------|---------------|
| `TestNewQuoteServer` | Создание сервера |
| `TestInitialPrices` | Начальные цены SBER, BTC, ETH |
| `TestPriceIsPositive` | Все цены > 0 |
| `TestUnknownSymbol` | Неизвестный символ не существует |
| `TestAllSymbolsExist` | Все обязательные символы есть |

### HT сервис (ht/main_test.go):

| Тест | Что проверяет |
|------|---------------|
| `TestDefaultGRPCAddress` | Дефолтный адрес localhost:50051 |
| `TestCustomGRPCAddress` | Кастомный адрес из env |
| `TestExpectedSymbols` | Список символов BTC, ETH, SBER |
| `TestHTTPPort` | Порт 8080 |
| `TestQuoteEndpoint` | Формат endpoint /quotes |

---

## 🐛 Отладка упавших тестов

### Если тесты не проходят:

```bash
# 1. Запустить с подробным выводом
go test -v ./...

# 2. Запустить конкретный тест
go test -run TestNewQuoteServer -v

# 3. Посмотреть логи Docker
docker compose logs ft
docker compose logs ht
```

---

## 🔧 Добавление новых тестов

### Пример нового теста:

```go
// В main_test.go или ht/main_test.go
func TestМояФункция(t *testing.T) {
    // Arrange (подготовка)
    expected := "значение"

    // Act (выполнение)
    result := МояФункция()

    // Assert (проверка)
    if result != expected {
        t.Errorf("Ожидалось %s, получено %s", expected, result)
    }
}
```

---

## 📈 Дальнейшее развитие

### Что можно добавить:

- [ ] Тесты с mock gRPC сервером
- [ ] Benchmark тесты (производительность)
- [ ] Тесты с разными сценариями ошибок
- [ ] E2E тесты с реальным UI
- [ ] Нагрузочные тесты (k6, wrk)
- [ ] Мониторинг покрытия тестами (codecov.io)

---

## 💡 Полезные команды

```bash
# Запустить только быстрые тесты
go test -short ./...

# Запустить тесты в параллель
go test -parallel 4 ./...

# Бенчмарк тесты
go test -bench=. ./...

# Проверка race conditions
go test -race ./...
```

---

## 🎓 Документация

- [Testing в Go](https://go.dev/doc/tutorial/add-a-test)
- [GitHub Actions](https://docs.github.com/en/actions)
- [Docker Compose](https://docs.docker.com/compose/)

---

**Вопросы?** Открывай issue на GitHub!
