# 🌥️ Работа с Cloud Agent в Cursor

## Цель

Работать **полностью в Cursor Cloud Agent** - разработка, тестирование, просмотр в браузере.

---

## 🎯 Архитектура

```
┌─────────────────────────────────────────────────┐
│           Cursor (на вашем экране)              │
│  ┌──────────────────────────────────────────┐   │
│  │  Cloud Agent (удаленный контейнер)       │   │
│  │  ├─ Код проекта                          │   │
│  │  ├─ Git операции                         │   │
│  │  └─ SSH туннель → Production             │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │  Simple Browser                          │   │
│  │  http://localhost:8081 → Adminer         │   │
│  │  http://localhost:3001 → UI              │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

---

## 🚀 Быстрый старт

### Шаг 1: Настроить доступ к серверу

В терминале Cloud Agent:

```bash
# Установить переменные окружения
export SERVER_HOST=your-server-ip
export SERVER_USER=your-username

# Проверить SSH ключи (должны быть настроены в GitHub Secrets)
ssh $SERVER_USER@$SERVER_HOST "echo 'Подключение успешно!'"
```

### Шаг 2: Запустить туннель

```bash
# В терминале Cloud Agent (оставьте его открытым)
./scripts/tunnel-to-production.sh
```

**Терминал "повиснет"** - это нормально! Туннель работает.

### Шаг 3: Открыть в Cursor Simple Browser

**Способ 1: Через Command Palette**

```
1. Ctrl+Shift+P (Cmd+Shift+P на Mac)
2. Введите: "Simple Browser: Show"
3. URL: http://localhost:8081
```

**Способ 2: Через Ports панель**

```
1. View → Ports (в меню Cursor)
2. Порты должны появиться автоматически
3. Правый клик → "Open in Browser"
```

---

## 📋 Доступные сервисы

После запуска туннеля, в Cursor Simple Browser:

| URL | Сервис | Описание |
|-----|--------|----------|
| `http://localhost:8081` | **Adminer** | Веб-интерфейс БД |
| `http://localhost:3001` | **UI** | React котировки |
| `http://localhost:8080/quotes` | **API** | REST API |
| `localhost:5432` | **PostgreSQL** | База данных (для psql) |

---

## 🗄️ Работа с Adminer

### Логин:
```
Server:   postgres
Username: admin
Password: secret123
Database: quotopia
```

### Что можно делать:
- ✅ Просматривать таблицы
- ✅ Добавлять записи
- ✅ Выполнять SQL запросы
- ✅ Экспортировать данные

### Примеры SQL:

```sql
-- Все пользователи
SELECT * FROM users;

-- Все инструменты
SELECT * FROM instruments;

-- Добавить новый инструмент
INSERT INTO instruments (symbol, name, initial_price, volatility, created_by) 
VALUES ('TSLA', 'Tesla', 245.50, 0.4, 1);

-- История изменений
SELECT * FROM instruments_audit_view LIMIT 10;
```

---

## 🎨 Работа с UI в Simple Browser

### Открыть UI:
```
http://localhost:3001
```

### Что увидите:
- 📊 Котировки в реальном времени
- 💰 BTC, ETH, SBER, AAPL, GOOGL
- ⏱️ Обновление каждые 2 секунды

### Проверить API напрямую:
```bash
# В терминале Cloud Agent (отдельное окно)
curl http://localhost:8080/quotes
```

---

## 🔧 Полезные скрипты

### `scripts/tunnel-to-production.sh`

Создаёт SSH туннели к production:

```bash
# С переменными окружения
export SERVER_HOST=1.2.3.4
export SERVER_USER=ubuntu
./scripts/tunnel-to-production.sh

# Или inline
SERVER_HOST=1.2.3.4 SERVER_USER=ubuntu ./scripts/tunnel-to-production.sh
```

### `scripts/open-in-cursor.sh`

Показывает инструкции:

```bash
./scripts/open-in-cursor.sh
```

---

## 🎯 Workflow для разработки

### 1. Утром (начало работы)

```bash
# Терминал 1: Запустить туннель
./scripts/tunnel-to-production.sh

# Терминал 2: Разработка
git status
code .
# Пишете код...
```

### 2. Проверка изменений

```bash
# 1. Закоммитить и запушить
git add .
git commit -m "Add feature"
git checkout main
git merge cursor/docker-cloud-local-worktree-f00c
git push origin main

# 2. Подождать CI/CD (~3 мин)

# 3. Обновить в браузере (localhost:3001)
# Увидите изменения!
```

### 3. Вечером (завершение)

```bash
# Ctrl+C в терминале с туннелем
# Готово! Можно закрыть Cursor
```

---

## 📱 Использование нескольких браузеров

Cursor позволяет открыть **несколько Simple Browser** одновременно:

```
Ctrl+Shift+P → "Simple Browser: Show"
  URL: http://localhost:8081  (Adminer)

Ctrl+Shift+P → "Simple Browser: Show"  
  URL: http://localhost:3001  (UI)

Ctrl+Shift+P → "Simple Browser: Show"
  URL: http://localhost:8080/quotes (API JSON)
```

**Или:**

Используйте вкладки - в Simple Browser есть адресная строка, можно менять URL!

---

## 🐛 Troubleshooting

### Проблема: "Connection refused"

**Причина:** Туннель не запущен.

**Решение:**
```bash
./scripts/tunnel-to-production.sh
```

### Проблема: "Permission denied (publickey)"

**Причина:** SSH ключи не настроены.

**Решение:**
```bash
# Проверить что ключ есть
ls -la ~/.ssh/

# Добавить ключ на сервер (с вашего компьютера, не Cloud Agent)
ssh-copy-id user@server

# Или скопировать вручную
cat ~/.ssh/id_rsa.pub
# Вставить в server:~/.ssh/authorized_keys
```

### Проблема: "Туннель отваливается"

**Причина:** Нестабильное соединение или сервер перезагружается.

**Решение:**
```bash
# Автореконнект скрипт
while true; do
    ./scripts/tunnel-to-production.sh
    echo "Туннель упал, переподключение через 5 сек..."
    sleep 5
done
```

### Проблема: "Порты не появляются в Cursor"

**Причина:** Cursor не видит порты автоматически.

**Решение:**

1. **Вручную добавить порт:**
   ```
   View → Ports → Forward a Port → 8081
   ```

2. **Или открыть напрямую:**
   ```
   Ctrl+Shift+P → "Simple Browser: Show"
   URL: http://localhost:8081
   ```

---

## 💡 Советы

### Использовать .env файл

Создайте `.env` (не коммитить!):

```bash
# .env
SERVER_HOST=your-server-ip
SERVER_USER=your-username
```

Загружать:
```bash
source .env
./scripts/tunnel-to-production.sh
```

### Алиасы для удобства

Добавьте в `~/.bashrc` (в Cloud Agent):

```bash
alias tunnel='./scripts/tunnel-to-production.sh'
alias open-ui='echo "Open: http://localhost:3001"'
alias open-db='echo "Open: http://localhost:8081"'
```

### Держать туннель в tmux

```bash
# Установить tmux (если нет)
apt-get install tmux

# Запустить туннель в фоне
tmux new -d -s tunnel './scripts/tunnel-to-production.sh'

# Посмотреть статус
tmux ls

# Присоединиться
tmux attach -t tunnel
```

---

## 🚀 Advanced: CI/CD в Cloud Agent

Можете даже смотреть CI/CD прямо в Cursor:

```bash
# Смотреть GitHub Actions
gh run list
gh run watch

# Или открыть в Simple Browser
# https://github.com/Greatmars95/ft/actions
```

---

## ✅ Checklist

- [ ] Настроены переменные `SERVER_HOST` и `SERVER_USER`
- [ ] SSH ключи работают
- [ ] Туннель запущен (`./scripts/tunnel-to-production.sh`)
- [ ] Simple Browser открыт в Cursor
- [ ] Adminer доступен (localhost:8081)
- [ ] UI доступен (localhost:3001)
- [ ] Можете работать полностью в Cursor! 🎉

---

**Всё в одном месте - Cloud Agent + Cursor = 💪**
