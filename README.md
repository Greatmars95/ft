# Quotopia - Real-time Quote Streaming Platform

Микросервисная архитектура для потоковой передачи финансовых котировок в реальном времени.

## 🏗️ Архитектура

```
┌─────────────────┐
│   UI (React)    │  ← Фронтенд на React + Vite + TailwindCSS
│   Port: 3001    │     Обновляет котировки каждые 2 секунды
└────────┬────────┘
         │ HTTP/REST
         ↓
┌─────────────────┐
│   HT (Gin)      │  ← HTTP Gateway (REST ↔ gRPC мост)
│   Port: 8080    │     Преобразует HTTP запросы в gRPC стримы
└────────┬────────┘
         │ gRPC Stream
         ↓
┌─────────────────┐
│   FT (gRPC)     │  ← Core Service - генератор котировок
│   Port: 50051   │     Стримит котировки BTC, ETH, SBER
└─────────────────┘
```

## 🔧 Технологии

- **FT (Quote Generator)**: Go + gRPC + Protocol Buffers
- **HT (HTTP Gateway)**: Go + Gin + gRPC Client
- **UI**: React 18 + Vite + TailwindCSS
- **Infrastructure**: Docker + Docker Compose + Nginx

## 🚀 Быстрый старт

### Запуск всех сервисов через Docker Compose

```bash
# Сборка и запуск всех сервисов
make up

# Или напрямую через docker-compose
docker-compose up --build
```

После запуска:
- UI доступен на `http://localhost:3001`
- HT API доступен на `http://localhost:8080/quotes`
- FT gRPC сервер на `localhost:50051`

### Остановка сервисов

```bash
make down
```

### Просмотр логов

```bash
make logs
```

## 📦 Структура проекта

```
ft/
├── main.go                # FT - gRPC сервер (генератор котировок)
├── proto/
│   └── quotes.proto       # Protocol Buffers схема
├── ht/
│   ├── main.go           # HT - HTTP Gateway
│   ├── Dockerfile
│   ├── go.mod
│   └── go.sum
├── ui/
│   ├── src/
│   │   ├── App.jsx       # React приложение
│   │   └── index.jsx
│   ├── nginx.conf        # Nginx конфиг с проксированием
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml    # Оркестрация сервисов
├── Dockerfile            # Dockerfile для FT
└── Makefile             # Утилиты для разработки
```

## 🔍 Детали реализации

### FT (Quote Generator)
- Генерирует котировки для BTC, ETH, SBER
- Изменение цены: ±0.1% каждую секунду
- Использует gRPC server-side streaming

### HT (HTTP Gateway)
- Принимает HTTP GET `/quotes`
- Открывает gRPC стрим к FT
- Собирает последние котировки за 5 секунд
- Возвращает JSON с актуальными ценами
- Добавляет CORS headers

### UI (Frontend)
- SPA на React с Vite
- Polling каждые 2 секунды
- Отображает цены в реальном времени
- Nginx проксирует `/quotes` → `ht:8080`

## 📝 Локальная разработка

### Генерация proto файлов

```bash
make proto
```

### Запуск FT локально

```bash
make run
```

### Запуск HT локально

```bash
cd ht
go run main.go
```

### Запуск UI локально

```bash
cd ui
npm install
npm run dev
```

## 🐛 Что было исправлено

### Критические проблемы:

1. ✅ **HT сервис не был в docker-compose.yml** - добавлен с правильными зависимостями
2. ✅ **Конфликт портов** - HT теперь на 8080, UI прокси настроен правильно
3. ✅ **localhost в Docker** - изменено на `ft:50051` через env переменную
4. ✅ **Proto файлы не генерировались** - добавлена генерация в Dockerfile
5. ✅ **Несовместимые модули Go** - унифицированы пути импорта `ft-mt`
6. ✅ **Отсутствие nginx proxy** - создан nginx.conf с проксированием
7. ✅ **Неэффективный стриминг** - теперь собираются только последние значения
8. ✅ **Docker build context** - настроен правильно для всех сервисов
9. ✅ **Отсутствие сети** - создана bridge сеть `quotopia-net`

### Дополнительные улучшения:

- Добавлен `.dockerignore` для оптимизации сборки
- Добавлены CORS headers в HT
- Улучшено логирование во всех сервисах
- Добавлены make команды для Docker Compose
- Создана документация

## 📊 API

### GET /quotes

Возвращает актуальные котировки:

```json
[
  {
    "symbol": "BTC",
    "price": 95423.45,
    "timestamp": 1704988123456
  },
  {
    "symbol": "ETH",
    "price": 2651.32,
    "timestamp": 1704988123456
  },
  {
    "symbol": "SBER",
    "price": 275.67,
    "timestamp": 1704988123456
  }
]
```

## 🔐 Порты

- `3001` - UI (Nginx)
- `8080` - HT (HTTP Gateway)
- `50051` - FT (gRPC Server)

## 📌 TODO

- [ ] Добавить Prometheus metrics
- [ ] Добавить health checks
- [ ] Добавить graceful shutdown
- [ ] Добавить rate limiting
- [ ] Добавить WebSocket для real-time updates
- [ ] Добавить Redis для кеширования
- [ ] Добавить Kubernetes манифесты
- [ ] Добавить CI/CD pipeline

## 📄 Лицензия

MIT
