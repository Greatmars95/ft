# 🏛️ Quotopia

**DevOps Playground** — микросервисная платформа для изучения современного стека технологий.

Quotopia — это учебный проект, демонстрирующий микросервисную архитектуру с gRPC взаимодействием, REST API, Docker контейнеризацией и будущей интеграцией с Kubernetes, Kafka, мониторингом и GitOps.

---

## 🏗️ Архитектура

```
┌──────────────────┐      gRPC       ┌──────────────────┐
│                  │   (stream)      │                  │
│   FT             │ ───────────────►│   CORE           │
│   :50051         │   котировки     │   :8080          │
│                  │                 │                  │
│  Генерирует      │                 │  Получает        │
│  котировки       │                 │  + REST API      │
│                  │                 │                  │
└──────────────────┘                 └──────────────────┘
                                            │
                                            │ HTTP
                                            ▼
                                     ┌──────────────┐
                                     │   Клиенты    │
                                     │   curl/web   │
                                     └──────────────┘
```

### Компоненты:

**FT (Feed/Ticker)** — сервис генерации котировок
- gRPC сервер на порту `:50051`
- Генерирует котировки каждую секунду
- Стримит данные всем подключённым клиентам
- Тикеры: `SBER`, `BTC`, `ETH`

**Core** — REST API сервис
- HTTP сервер на порту `:8080`
- gRPC клиент (подключается к FT)
- Получает котировки в реальном времени
- Отдаёт их через REST API

**Proto** — gRPC контракт
- Описывает структуру данных и методы
- Автоматическая генерация кода для Go

---

## 🚀 Быстрый старт

### Требования

- Docker 20.10+
- Docker Compose 2.0+
- Make (опционально, но удобно)

### Запуск

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd quotopia

# 2. Запустить сервисы
make run

# Или без Make:
docker-compose up --build -d
```

### Проверка работы

```bash
# Проверка здоровья
curl http://localhost:8080/health

# Все котировки
curl http://localhost:8080/quotes

# Одна котировка
curl http://localhost:8080/quotes/BTC

# Или через Make:
make test
```

### Остановка

```bash
make stop

# Или:
docker-compose down
```

---

## 📁 Структура проекта

```
quotopia/
├── proto/
│   └── quotes.proto       # gRPC контракт
│
├── ft/                    # FT сервис (генератор котировок)
│   ├── main.go           # Код gRPC сервера
│   ├── go.mod            # Go зависимости
│   ├── Dockerfile        # Образ для FT
│   └── gen/              # Автогенерируемый код (в .gitignore)
│
├── core/                  # Core сервис (REST API)
│   ├── main.go           # Код HTTP + gRPC клиента
│   ├── go.mod            # Go зависимости
│   ├── Dockerfile        # Образ для Core
│   └── gen/              # Автогенерируемый код (в .gitignore)
│
├── docker-compose.yml     # Оркестрация сервисов
├── Makefile              # Команды управления
├── .gitignore            # Исключения для git
└── README.md             # Этот файл
```

---

## 🔧 Описание сервисов

### FT (Feed/Ticker)

**Что делает:**
- Генерирует котировки для тикеров `SBER`, `BTC`, `ETH`
- Каждую секунду обновляет цены на случайную величину (-0.1% до +0.1%)
- Отправляет обновления всем подключённым клиентам через gRPC stream

**Технологии:**
- Go 1.23
- gRPC server
- Protocol Buffers

**Порт:** `:50051` (gRPC)

**Пример лога:**
```
🚀 FT сервис запущен на :50051
📊 Генератор котировок запущен
📈 SBER: 275.50 → 275.52 (0.01%)
📈 BTC: 95400.00 → 95395.23 (-0.01%)
📈 ETH: 2650.20 → 2650.45 (0.01%)
✅ Новый клиент подключился к стриму
```

### Core

**Что делает:**
- Подключается к FT по gRPC
- Получает стрим котировок в реальном времени
- Хранит последние значения в памяти
- Отдаёт котировки через REST API

**Технологии:**
- Go 1.23
- Gin (HTTP фреймворк)
- gRPC client

**Порт:** `:8080` (HTTP)

**Пример лога:**
```
🚀 Core сервис запущен на :8080
🔌 Подключаюсь к FT сервису: ft:50051
📡 Открываю стрим котировок...
✅ Стрим открыт, получаю котировки...
💾 Обновлена котировка: BTC = 95432.12
💾 Обновлена котировка: ETH = 2651.33
💾 Обновлена котировка: SBER = 275.48
```

---

## 📡 API Reference

### Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/health` | Проверка здоровья сервиса |
| `GET` | `/quotes` | Все котировки |
| `GET` | `/quotes/:symbol` | Одна котировка по символу |

### Примеры запросов

#### 1. Health Check

```bash
curl http://localhost:8080/health
```

**Ответ:**
```json
{
  "status": "healthy",
  "time": "2025-01-11T22:30:00Z"
}
```

#### 2. Все котировки

```bash
curl http://localhost:8080/quotes
```

**Ответ:**
```json
{
  "BTC": 95432.12,
  "ETH": 2651.33,
  "SBER": 275.48
}
```

#### 3. Одна котировка

```bash
curl http://localhost:8080/quotes/BTC
```

**Ответ:**
```json
{
  "symbol": "BTC",
  "price": 95432.12
}
```

#### 4. Несуществующая котировка

```bash
curl http://localhost:8080/quotes/DOGE
```

**Ответ (404):**
```json
{
  "error": "not found",
  "symbol": "DOGE"
}
```

---

## 🛠️ Make команды

```bash
make help      # Показать справку
make proto     # Генерация gRPC кода из .proto
make build     # Сборка Docker образов
make run       # Запуск сервисов (build + up)
make stop      # Остановка сервисов
make restart   # Перезапуск
make logs      # Просмотр логов
make status    # Статус контейнеров
make test      # Тест API
make clean     # Полная очистка (контейнеры, образы, volumes)
```

---

## 🔍 Как это работает

### 1. gRPC Stream (FT → Core)

```
FT запускается
    ↓
FT генерирует котировки каждую секунду
    ↓
Core подключается к FT по gRPC
    ↓
Core получает stream котировок
    ↓
Core сохраняет в памяти (map[symbol]price)
```

### 2. REST API (Client → Core)

```
Пользователь делает curl http://localhost:8080/quotes
    ↓
Core обрабатывает HTTP запрос (Gin router)
    ↓
Core читает из памяти последние котировки
    ↓
Core возвращает JSON ответ
```

### 3. Docker Networking

```
Docker создаёт сеть quotopia-network
    ↓
Контейнер FT получает DNS имя "ft"
    ↓
Контейнер Core обращается к "ft:50051"
    ↓
Docker DNS резолвит "ft" в IP контейнера FT
```

---

## 🧪 Тестирование

### Автоматические тесты

```bash
make test
```

### Ручное тестирование

```bash
# 1. Запустить сервисы
make run

# 2. Смотреть логи в реальном времени
make logs

# 3. В другом терминале делать запросы
curl http://localhost:8080/quotes
curl http://localhost:8080/quotes/BTC

# 4. Проверить статус контейнеров
make status
```

### Проверка gRPC напрямую (опционально)

Требует установки `grpcurl`:

```bash
# Список сервисов
grpcurl -plaintext localhost:50051 list

# Вызов метода
grpcurl -plaintext localhost:50051 QuoteService/StreamQuotes
```

---

## 🐛 Troubleshooting

### Проблема: "connection refused" при запуске

**Решение:**
```bash
# Проверить что Docker запущен
docker ps

# Проверить логи
make logs

# Пересобрать образы
make clean && make run
```

### Проблема: Core не получает котировки

**Решение:**
```bash
# Проверить что FT запустился
docker logs quotopia-ft

# Проверить что Core подключился
docker logs quotopia-core

# Должна быть строка: "✅ Стрим открыт, получаю котировки..."
```

### Проблема: Порт уже занят

**Решение:**
```bash
# Найти процесс на порту 8080
lsof -i :8080

# Или для 50051
lsof -i :50051

# Убить процесс
kill -9 <PID>
```

---

## 📈 Roadmap

- [x] **Phase 1:** FT + Core (gRPC streaming)
- [ ] **Phase 2:** Kafka между сервисами
- [ ] **Phase 3:** PostgreSQL для хранения истории
- [ ] **Phase 4:** Kubernetes манифесты (Deployments, Services)
- [ ] **Phase 5:** Terraform для инфраструктуры
- [ ] **Phase 6:** GitOps (FluxCD/ArgoCD)
- [ ] **Phase 7:** Мониторинг (Prometheus + Grafana)
- [ ] **Phase 8:** Трейсинг (Jaeger/Tempo)
- [ ] **Phase 9:** Service Mesh (Istio/Linkerd)
- [ ] **Phase 10:** UI Dashboard (React/Vue)

---

## 📚 Обучение

### Что можно изучить на этом проекте

**Микросервисы:**
- Разделение ответственности (FT = генератор, Core = API)
- Межсервисное взаимодействие через gRPC
- Retry логика и fault tolerance

**gRPC:**
- Protocol Buffers
- Server streaming
- Code generation

**Docker:**
- Multi-stage builds
- Docker Compose
- Networking между контейнерами

**DevOps:**
- Makefile для автоматизации
- Логирование и мониторинг
- Health checks

**Go:**
- Concurrency (goroutines, channels)
- Mutex для thread-safe операций
- HTTP серверы (Gin)
- gRPC клиент и сервер

---

## 🤝 Contributing

Проект создан для обучения. Вы можете:
- Добавить новые тикеры
- Реализовать WebSocket для реалтайм обновлений
- Добавить персистентность (PostgreSQL/Redis)
- Создать UI dashboard
- Добавить метрики Prometheus
- Написать тесты

---

## 📝 Лицензия

MIT License

---

## 📧 Контакты

Вопросы и предложения приветствуются!

---

**Создано для изучения DevOps и микросервисной архитектуры**
